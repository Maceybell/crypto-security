let alphabet = "abcdefghijklmnopqrstuvwxyz"
let cipher = "mnopqrstuvwxyzabcdefghijkl"

let decode = (str) => {
   let newStr = str.replace('a', 'm')
   newStr = newStr.replace('b','n')
   newStr = newStr.replace('c', 'o')
   newStr = newStr.replace('d', 'p')
   newStr = newStr.replace('e','q')
   newStr = newStr.replace('f', 'r')
   newStr = 

   console.log (newStr)
   } 



decode('maceytaylor')
decode('ilovecoding')
decode('everything everywhere all at once')